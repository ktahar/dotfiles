#!/bin/bash
if command -v alacritty &> /dev/null; then
    alacritty
elif command -v urxvt_local &> /dev/null; then
    urxvt_local
elif command -v gnome-terminal &> /dev/null; then
    gnome-terminal
else
    xterm
fi
