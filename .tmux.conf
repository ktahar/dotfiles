# Kosuke Tahara's tmux configuration

# Plug-ins
run-shell ~/.tmux/plugins/tmux-resurrect/resurrect.tmux
set -g @resurrect-strategy-vim 'session'
set -g @resurrect-restore 'C-e' # prefix E to restore, instead of R.

unbind C-r

# prefix <C-t> instead of <C-b>
set -g prefix C-t
unbind C-b
# stroke <C-t> twice to send <C-t> to a program inside tmux
bind C-t send-prefix

# prefix-r to load .tmux.conf
bind r source-file ~/.tmux.conf \; display-message "reloaded ~/.tmux.conf"

# window and pane index 1-origin
set -g base-index 1
setw -g pane-base-index 1

# color
set -g default-terminal "screen-256color"

# split
bind | split-window -h
bind - split-window -v

# pane selection
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# pane resize
bind -r H resize-pane -L 3
bind -r J resize-pane -D 3
bind -r K resize-pane -U 3
bind -r L resize-pane -R 3
